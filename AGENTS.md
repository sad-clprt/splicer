# AGENTS.md

## Code Map
- `app/` - application source
- `tests/` - automated tests

## Conventions
- Use Python `import` and `from ... import ...` syntax with absolute `app.*` imports.
- Use `.py` extensions for all Python modules.
